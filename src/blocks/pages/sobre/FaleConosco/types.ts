import type { AcfLink } from '../../../../utils';

export interface FaleConoscoSobreProps {
  patternUrl?: string;
  eyebrow?: string;
  title?: string;
  description?: string;
  button?: AcfLink | null;
  button2?: AcfLink | null;
}
