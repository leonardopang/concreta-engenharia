import type { AcfLink } from '../../../../utils';

export interface FaleConoscoProps {
  patternUrl?: string;
  eyebrow?: string;
  title?: string;
  description?: string;
  titleButton?: string;
  button?: AcfLink | null;
}
